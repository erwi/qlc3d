To compile:

Edit the makefile.def file to set paths and compiler flags, then runQlc3d make all.

Also required the SpaMtrix library for sparse matrix stuff. Download this from https://github.com/erwi/SpaMtrix

